# GitHub Configuration
REPO=username/repository-name
BRANCH=main
GITHUB_TOKEN=YOUR_GITHUB_TOKEN

# OpenAI Configuration
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-4.1-mini

# Semgrep Configuration for default rules
# Options: 'auto' (recommended), 'p/security-audit', 'p/owasp-top-ten', 'p/cwe-top-25', 'p/ci'
# 'auto' automatically selects rules based on detected languages and avoids Pro-only rules
RULES_PATH=auto

# Output Configuration
OUTPUT_FILE=results.json
AI_ANALYSIS_FILE=ai_analysis.json
TEMP_DIR=temp_repo

# SSL Configuration (for corporate environments with SSL proxies)
# Set to 'true' if you encounter SSL certificate verification errors with:
# - GitHub API downloads
# - Semgrep rule downloads from semgrep.dev
DISABLE_SSL_VERIFY=false

